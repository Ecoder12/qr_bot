<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Ticket</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #FF6F00;
            color: #175c43;
        }

        .container {
            width: 80%;
            margin: auto;
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            color: #FF6F00;
            font-size: 28px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border: 1px solid #ddd;
        }

        th {
            background-color: #FF6F00;
            color: white;
        }

        .qr-section {
            text-align: center;
            margin-top: 30px;
        }

        .qr-section img {
            margin: 20px 0;
            width: 200px;
            height: 200px;
        }

        .download-btn {
            background-color: #FF6F00;
            color: white;
            padding: 12px 20px;
            font-size: 18px;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
        }

        .download-btn:hover {
            background-color: #FF8C1A;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Your Ticket Details</h2>
        
        <table>
            <tr>
                <th>Campus Ambassador ID</th>
                <td><%= ticketData.campusAmbassadorID %></td>
            </tr>
            <tr>
                <th>Full Name</th>
                <td><%= ticketData.fullName %></td>
            </tr>
            <tr>
                <th>Phone Number</th>
                <td><%= ticketData.phoneNumber %></td>
            </tr>
            <tr>
                <th>Email Address</th>
                <td><%= ticketData.emailAddress %></td>
            </tr>
            <tr>
                <th>Ticket Type</th>
                <td><%= ticketData.ticketType %></td>
            </tr>
            <tr>
                <th>Category</th>
                <td><%= ticketData.category %></td>
            </tr>
            <tr>
                <th>Payment Method</th>
                <td><%= ticketData.paymentMethod %></td>
            </tr>
        </table>
        
        <div class="qr-section">
            <h3>QR Code</h3>
            <img src="<%= ticketData.qrCode %>" alt="QR Code">
            <p>Scan this QR code to verify your ticket!</p>
            
            <a href="<%= ticketData.qrCode %>" class="download-btn" download="ticket_qr.png">
                Download QR Code
            </a>
        </div>
    </div>
</body>
</html>
